      - name: Setup Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24
          # le quitamos el cache para que no falle

      - name: Instalar dependencias
        run: npm install
